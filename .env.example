# Copy to .env and fill in your key. Required for LLM reasoning (Diagnosis/Planner)
# and the auto-heal toggle. Without it, the app runs in manual mode with KG-derived
# diagnosis only.
OPENAI_API_KEY=sk-replace-with-your-key
